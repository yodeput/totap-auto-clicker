<?xml version="1.0" encoding="utf-8"?>
<!--
  AccessibilityService configuration for Totap (spec §4.5).
  - canPerformGestures: REQUIRED — enables dispatchGesture() (the actual taps).
  - We request no event feedback and no package filter; we only dispatch, we don't read.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:notificationTimeout="0" />

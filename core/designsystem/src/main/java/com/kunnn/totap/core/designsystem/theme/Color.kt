package com.kunnn.totap.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// Totap brand gradient endpoints (from the launcher icon)
val TotapYellow = Color(0xFFFFEB3B)     // #FFEB3B — gradient start
val TotapChartreuse = Color(0xFFDFFF00) // #DFFF00 — gradient end
val TotapGradientAngleDeg = 288f        // gradient angle, degrees

// Accent / symbol
val TotapInk = Color(0xFF1A1A1A)        // near-black for the hand-cursor symbol & primary buttons
val TotapSurface = Color(0xFFFFFFFF)    // white cards over the gradient
val TotapOnInk = Color(0xFFFFFFFF)      // content on black buttons
